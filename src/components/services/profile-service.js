import config from '../../config';
import TokenService from '../services/token-service';

const ProfileService = {
  /**
		 * Need function to update acception/rejection 
		 * table
  */
  updateAcceptionTable: () => {},

  getGlobalScores: () => {},

  getProfileScores: () => {},

  

}

export default ProfileService;